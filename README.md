# Hide and Seek: Revisiting DNS-based User Tracking

Here we share our codes about **DSCorr** and **prototype of LDPResolve**. See corresponding folders to find the details.

* Folder `dscorr/` is the code of domain embedding model and DSCorr tracking method.
* Folder `ldpresolve/` is the code and configuration of LDPResolve prototype.
